{
  "name": "@fm/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test league-integration.test.ts && node --test league-api.test.ts && node --test league-auction.test.ts && node --test league-season.test.ts && node --test league-facilities.test.ts && node --test league-transfers.test.ts && node --test league-training.test.ts && node --test league-sharpness.test.ts",
    "serve": "node league-server.ts",
    "seed-demo": "node scripts/seed-demo.ts",
    "db:test:up": "docker start fm-test-pg 2>/dev/null || docker run -d --name fm-test-pg -e POSTGRES_PASSWORD=fm -e POSTGRES_DB=fm_test -p 54329:5432 postgres:16-alpine",
    "db:test:down": "docker rm -f fm-test-pg"
  },
  "dependencies": {
    "@fm/engine": "workspace:*",
    "@fastify/cookie": "^11.0.2",
    "@fastify/static": "^9.1.3",
    "fastify": "^5.9.0",
    "pg": "^8.22.0",
    "pg-boss": "^12.24.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "@types/pg": "^8.20.0",
    "typescript": "^5.8.0"
  }
}
